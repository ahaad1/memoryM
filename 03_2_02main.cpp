#include <stdlib.h>
#include <stdio.h>
#include "03_2_02task.cpp"
// #include "os_mem.h"

int main(){
    memory_manager_t mm;
    setup_memory_manager(&mm);
    
    // mm.create(1000, 1);
    // mm.alloc(10);
    // mm.alloc(10);
    // mm.alloc(10);
    // mm.alloc(10);
    return 0;
}